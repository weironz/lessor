# 路线图

里程碑制，不排日历。**这里是开发计划的唯一事实来源** ——
做完的条目移入 [ROADMAP-done.md](ROADMAP-done.md)（带完成日期与验收证据）。
每条决策的"为什么"在 [docs/design-and-plan.md](docs/design-and-plan.md)。

两种运行形态：**现场**（field，笔记本临时用）与**常驻**（resident，长期服务）。
标注在各条目上。

## M9 · 真机 BMC 验证（现场形态发布 gate）

**前置风险要先认**：真机 BMC 行为全部未验证（现有矩阵是 VMware 固件 +
Linux 客户端）。若 B300 出厂 DHCP 关闭，"插线即得"第一步不成立 ——
兜底是 discovery（RMCP + 邻居表），本来就为不来要地址的机器设计。

拆成两半，因为**两半的前置条件完全不同**：取证只要有人能在机房里跑个进程，
闭环要能独占一个网段。前者现在就能做，后者得等窗口。两半都做完才算过 gate。

### M9a · 远程取证（不碰生产网络，现在就能做）

DHCP 靠广播工作，过不去三层 —— Tailscale / VPN 连过去是发不了地址的。
而生产 BMC 网段十有八九已经有 DHCP 在跑，直接起就是两个 DHCP 抢答。
所以取证走 `--observe`：收包、记录、显示"本来会怎么答"，一个字节都不发。

摆在哪儿有两条路（[中继已支持](docs/dhcp-relay.md)）：

- **同二层**：机房里找一台和 BMC 同网段的机器跑 `--observe --capture`。
  不用动网络配置。
- **经中继**：在路由器上**多加一个** `ip helper-address` 指向一个
  `--observe` 的 lessord，它就能收到该网段每个 DHCP 请求的副本，
  而不必在目标网段上有机器。代价是要走路由器变更。

之后的步骤两条路一样：

1. 挂到攒够样本（等机器重启或续租触发握手）
2. 把捕获文件带回来 `--replay`，逐条过真正的决策层
3. **每个怪癖固化为回归测试或文档**（option 61 已是先例）；
   无法解码的报文优先处理 —— 那是我们还不认识的编码

**验收**：≥3 款真机 BMC（含 B300）的真实报文进了捕获文件并重放通过；
重放报告里无法解码的条目为 0，或每一条都有对应的测试与文档。

工具已就绪（`--capture` / `--replay` / `--observe`，见 CHANGELOG）——
这一项现在缺的只是机房里有人跑一次。

### M9b · 现场闭环（要能独占网段或维护窗口）

- ≥3 款真机 BMC（含 B300）：插线 → 拿到租约 → discovery 确认，闭环走通
- 顺带补上 M8 欠的整机走查：干净 Win11 + 非管理员账户 + 默认防火墙 +
  USB 网卡，按 [runbook](docs/field-runbook.md) 从零走一遍；拔网卡可恢复
- BMC 网页跳转是附带能力，**不设验收**

**验收**：三台机器各自插线即拿到租约（或 DHCP 关闭时被 discovery 找到），
界面上的 MAC↔IP 与机器标签逐一对得上。

## v1.0+ · 企业可靠性（常驻，以真实部署需求触发）

- **HA：多实例共享 PostgreSQL**（首选；自研同步协议只做兜底）——
  实例无状态化，冲突由数据库唯一约束仲裁；预估 1–2k 行（PG 后端 + 失效策略）
- 审计日志、权限分级
- 存储阶梯就此闭合：ephemeral → sqlite → 共享 PG

## v1.x · 协议扩展

- DHCPv6
- DDNS 联动（完整 DNS 服务器仍是非目标）

## 已否决

- **代码签名证书**（2026-08-28 决定不买）。后果要认下来：Windows 上
  下载来的未签名 exe 会被 SmartScreen 拦一道（"更多信息 → 仍要运行"），
  管得严的客户现场 EDR 可能直接拦死，那种场合只能改用命令行压缩包或
  容器镜像。相应地：
  - 安装包不是唯一交付形态，CLI 压缩包和镜像要一直保持可用
  - 现场文档要写清"被 SmartScreen 拦了怎么办"，别让人以为是坏了
  - 这个决定可以翻 —— 触发条件是有客户因为签名问题进不了场
